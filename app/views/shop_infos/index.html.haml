%h1 Listing shop_infos

%table
  %tr
    %th Name
    %th Address
    %th Url
    %th Lat
    %th Lng
    %th Photo
    %th Tel
    %th Shop category
    %th
    %th
    %th

  - @shop_infos.each do |shop_info|
    %tr
      %td= shop_info.name
      %td= shop_info.address
      %td= shop_info.url
      %td= shop_info.lat
      %td= shop_info.lng
      %td= link_to '店舗写真', shop_photo_path(shop_info.id)
      %td= shop_info.tel
      %td= ShopCategory.find(shop_info.shop_category_id).category
      %td= link_to 'Show', shop_info
      %td= link_to 'Edit', edit_shop_info_path(shop_info)
      %td= link_to 'Destroy', shop_info, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Shop info', new_shop_info_path
